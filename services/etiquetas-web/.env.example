# --- Desarrollo local (copiar a .env.local) ---
# IP de la PC donde corre etiquetas-api + supervisor
VITE_API_URL=http://10.1.102.8:8010

# --- Vercel (Settings → Environment Variables) ---
# Frontend: dejar VITE_API_URL vacío → usa /api (proxy incluido en este proyecto).
#
# Solo servidor (NO exponer al navegador):
# ETIQUETAS_API_ORIGIN=https://URL-PUBLICA-DE-TU-API
#   Ejemplo con túnel HTTPS hacia la PC impresora:8010
